/**
 * Service Transport
 * À compléter avec les données RATP / Vélib
 */

class TransportService {
  async getMetroStations(filters = {}) {
    // À implémenter
    return [];
  }

  async getVelibStations(filters = {}) {
    // À implémenter
    return [];
  }
}

module.exports = new TransportService();